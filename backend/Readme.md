# Job Application Tracker Backend

FastAPI backend for managing job applications, CVs and cover letters.

## Local setup

Create and activate a virtual environment:

```bash
python3 -m venv .venv
source .venv/bin/activate